#include <vector>
#include "vectorOperations.h"


void ballCollision(vector<double> &r, vector<double> &v,double R){
    for (int i = 0; i < r.size(); i++){
        for (int j = 0; j < r.size(); j++){

        }
    }
}
